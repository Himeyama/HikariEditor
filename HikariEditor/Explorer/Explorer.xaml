<Page
    x:Class="HikariEditor.Explorer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HikariEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <!-- ツールバー用ボタンの軽量スタイリング上書き。WinUI の Style には Resources が
             無いため、ブラシ上書きは Page.Resources 側に置く。このページのボタンはツール
             バーの4つだけなのでページ全体に適用してよい。-->
        <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#28808080"/>
        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#40808080"/>
        <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent"/>
        <Style x:Key="ExplorerToolButton" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
            <Setter Property="Padding" Value="6"/>
        </Style>
        <DataTemplate x:Key="ExplorerListViewTemplate" x:DataType="local:FileItem">
            <StackPanel Orientation="Horizontal" RightTapped="OnItemRightTapped">
                <StackPanel.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="場所を開く" Click="ClickOpenLocation" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE838;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="絶対パスのコピー" Click="ClickCopyAbsolutePath" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8C8;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="相対パスのコピー" Click="ClickCopyRelativePath" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8C8;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem Text="名前の変更" Click="ClickRename" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8AC;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="削除" Click="ClickDeleteItem" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE74D;" Foreground="#EF5350" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator Visibility="{x:Bind SvgVisibility}" />
                        <MenuFlyoutItem Text="テキストで開く" Click="ClickOpenAsText" Visibility="{x:Bind SvgVisibility}" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE130;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="WebView で開く" Click="ClickOpenAsWebView" Visibility="{x:Bind SvgVisibility}" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE774;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </StackPanel.ContextFlyout>
                <Grid>
                    <FontIcon Glyph="{x:Bind Icon1}" FontFamily="Segoe Fluent Icons" Foreground="{x:Bind Color1}" />
                    <FontIcon Glyph="{x:Bind Icon2}" FontFamily="Segoe Fluent Icons" Foreground="{x:Bind Color2}" />
                </Grid>
                <TextBlock Text="{x:Bind Name}" Margin="8, 0, 0, 0"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    <Grid RowDefinitions="Auto, *">
        <StackPanel Margin="16, 8, 16, 8" Spacing="4" Grid.Row="0">
            <TextBlock Text="Explorer" x:Uid="Explorer" FontSize="12"/>
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Style="{StaticResource ExplorerToolButton}" Click="ClickOpenExplorer">
                    <FontIcon FontSize="14" FontFamily="Segoe Fluent Icons" Glyph="&#xE197;" Foreground="#FFA000" />
                </Button>
                <Button Style="{StaticResource ExplorerToolButton}">
                    <FontIcon FontSize="14" FontFamily="Segoe Fluent Icons" Glyph="&#xE109;" Foreground="#2196F3" />
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Text="Add New File" x:Uid="AddNewFile" Click="ClickAddNewFile" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE130;" Foreground="#FFFFFF" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="Add New Folder" x:Uid="AddNewFolder" Click="ClickAddNewFolder" FontFamily="{StaticResource ContentControlThemeFontFamily}">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" Foreground="#FFD54F" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <Button Style="{StaticResource ExplorerToolButton}" Click="DeleteFileButtonClick">
                    <FontIcon FontSize="14" FontFamily="Segoe Fluent Icons" Glyph="&#xE74D;" Foreground="#EF5350" />
                </Button>
                <Button Style="{StaticResource ExplorerToolButton}" Click="ReloadButtonClick">
                    <FontIcon FontSize="14" FontFamily="Segoe Fluent Icons" Glyph="&#xE72C;" Foreground="#4CAF50" />
                </Button>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <TreeView x:Name="ExplorerTree"
                  ItemTemplate="{StaticResource ExplorerListViewTemplate}"
                  Expanding="ExplorerTreeExpanding"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
